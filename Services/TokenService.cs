namespace my_new_app.Services
{
    public class TokenService
    {
        public string CreateToken(TodoSignIn user){
            var claims = new List<Claim>{
                new Claim(ClaimTypes.)
            }
        }
    }
}